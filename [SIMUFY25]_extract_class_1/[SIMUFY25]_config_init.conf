# Конфигурация для использования без системных секретов

ENV=prod
pipeline_tag=sem-layer-beta
auth_mode=token
reader_adapter=d.remio.internal

# Проверьте этот ключ перед отправкой
key_fallback=J9a84jsT9akz==

log_output=/tmp/pipeline-sync.log
sync_strategy=delta_diff
